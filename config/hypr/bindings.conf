# Override or add keybindings here. Bindings live in bindings/*.conf (apps,
# clipboard, media, tiling-v2, utilities). This file is sourced last so you
# can unbind and rebind as needed.
#
# Examples:
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu
#
# bindd = SUPER SHIFT, A, ChatGPT, exec, omarchy-launch-webapp "https://chatgpt.com"
