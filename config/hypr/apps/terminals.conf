# Define terminal tag to style them uniformly
windowrule = tag +terminal, match:class (Alacritty|com.mitchellh.ghostty)
windowrule = tag -default-opacity, match:tag terminal
windowrule = opacity 0.98 1, match:tag terminal
