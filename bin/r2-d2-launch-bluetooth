#!/bin/bash

# Launch the R2-D2 bluetooth controls TUI (provided by bluetui).
# Also attempts to unblock bluetooth service if rfkill had blocked it.

rfkill unblock bluetooth
exec r2-d2-launch-or-focus-tui bluetui
