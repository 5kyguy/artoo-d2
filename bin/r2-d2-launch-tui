#!/bin/bash

# Launch the TUI command passed in as an argument in the default terminal with an org.r2d2.COMMAND app id for styling.

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.r2d2."$(basename $1)" -e "$1" "${@:2}"
