#!/bin/bash

# Refresh the ~/.config/chromium-flags.conf file from the Omarchy defaults.
# Chromium is used as backup browser only; no Google/Chromium account setup.

omarchy-refresh-config chromium-flags.conf
